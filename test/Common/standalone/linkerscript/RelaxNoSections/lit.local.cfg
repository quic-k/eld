if 'x86' in config.available_features:
    config.unsupported = True

if 'Hexagon' in config.eld_targets_to_build:
  if 'hexagon-unknown-linux' in config.target_triple:
     config.unsupported = True
